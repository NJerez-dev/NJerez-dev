# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository purpose

This is the special GitHub profile repository for the user `NJerez-dev` (the repo name matches the username, so `README.md` renders on https://github.com/NJerez-dev). It is **not** a software project — there is no source code, build system, tests, linters, or dependencies. The only content is `README.md`, written in Spanish, presenting Nicolás (an Industrial & Logistics engineer) and linking to two external portfolio repositories:

- `Analisis-Datos-Transporte-UM` (last-mile delivery analysis)
- `Logistics-data-portfolio` (inventory optimization analysis)

## Working in this repo

- Edits are almost always to `README.md`. Preserve the Spanish-language tone and the existing section structure (intro → especialización → proyectos destacados → enfoque).
- The README uses GitHub-flavored Markdown with emoji section headers; keep that style when adding sections.
- External project links point to other repos under the `NJerez-dev` GitHub account — do not rewrite them to point elsewhere.
- There is nothing to build, run, or test. Verification is visual: render the Markdown (e.g. via a preview) before committing.
